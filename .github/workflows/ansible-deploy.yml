name: Deploy with Ansible

on:
  push:
    branches:
      - main  # Запускать при пуше в ветку main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Клонирование репозитория
        uses: actions/checkout@v4

      - name: Установка Ansible
        run: |
          sudo apt update
          sudo apt install -y ansible

      - name: Установить SSH-ключ
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan github.com >> ~/.ssh/known_hosts
          eval $(ssh-agent -s)
          ssh-add ~/.ssh/id_rsa

      - name: Запуск Ansible Playbook
        run: |
          # Переменная для IP адреса вашего сервера
          SERVER_IP="54.145.135.174"
          
          # Выполнение playbook'а
          ansible-playbook -i $SERVER_IP, -u ubuntu --private-key ~/.ssh/id_rsa site.yml
